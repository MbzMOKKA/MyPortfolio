//Imports

//Exports
const paddings = {
    common: {
        modalHeader: `${8}px ${8}px ${8}px ${16}px`,
        modalMain: `${10}px ${20}px`,
        button: `${10}px`,
        list: `${20}px ${10}px`,
    },
    special: {
        navBar: `${0}px ${5}%`,
        navBtnItself: `${8}px ${0}px`,
        navBtnTxt: `${8}px ${0}px`,
        contactMethod: `${6}px`,
        contactInfo: `${12}px`,
    },
};

export default paddings;
