//Imports

//Exports
const margins = {
    common: {
        nameBottom: `${10}px`,
        nameSeparatorRight: `${15}px`,
        option: `${5}px ${0}px`,
        grid: `${20}px`,
    },
    special: { settingsInput: `${12}px ${0}px` },
};

export default margins;
